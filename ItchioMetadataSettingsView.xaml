<UserControl x:Class="ItchioMetadata.ItchioMetadataSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">
    
    <StackPanel Margin="20">
        <TextBlock Text="itch.io Metadata Provider Settings" 
                   FontSize="16" 
                   FontWeight="Bold" 
                   Margin="0,0,0,20"/>

        <CheckBox Content="Prefer itch.io description over existing" 
                  IsChecked="{Binding PreferItchioDescription}"
                  Margin="0,0,0,10"/>

        <CheckBox Content="Download screenshots for background images" 
                  IsChecked="{Binding DownloadScreenshots}"
                  Margin="0,0,0,10"/>

        <CheckBox Content="Automatically use first search result (background mode)" 
                  IsChecked="{Binding PreferFirstSearchResult}"
                  Margin="0,0,0,10"
                  ToolTip="When enabled, the first search result will be used automatically during background metadata downloads"/>

        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
            <TextBlock Text="Max search results:" 
                       VerticalAlignment="Center"
                       Margin="0,0,10,0"/>
            <TextBox Text="{Binding MaxSearchResults}" 
                     Width="60"
                     VerticalAlignment="Center"/>
        </StackPanel>

        <TextBlock Text="Note: This plugin scrapes data from itch.io game pages. Make sure to respect itch.io's terms of service." 
                   TextWrapping="Wrap"
                   Foreground="Gray"
                   FontSize="11"
                   Margin="0,20,0,0"/>
    </StackPanel>
</UserControl>
